{
  "firestore": { "rules": "firebase/firestore.rules" },
  "storage": { "rules": "firebase/storage.rules" },
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "**/.*", "README.md", "scripts/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  }
}
